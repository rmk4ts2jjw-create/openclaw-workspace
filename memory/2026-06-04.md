
## Workspace Restructure (10:30-10:40)

### Changes Made
- Removed `lovable-dev/` (old Lovable prototype, redundant)
- Moved `sprite_sheet.png` → `assets/`
- Moved stray `2026-05-12.md` → `memory/`
- Removed `.aider.chat.history.md` (Aider remnant)
- Added `mission-control-dashboard/` and `mission-control-dashboard-dev/` to .gitignore
- Created LaunchAgent for Production MC dashboard (port 3000, auto-start on boot)
- Created dev clone at `mission-control-dashboard-dev/` (port 3001, manual start)

### New Folder Structure
```
~/.openclaw/workspace/
├── mission-control-dashboard/      ← PRODUCTION (port 3000, LaunchAgent)
├── mission-control-dashboard-dev/  ← DEVELOPMENT (port 3001, manual)
├── assets/                         ← Shared assets (sprites, icons)
├── scripts/                        ← Backup scripts, health checks
├── data/                           ← Task data, incidents, cron errors
├── memory/                         ← Daily logs, memory files
├── skills/                         ← OpenClaw skills
├── logs/                           ← System logs
├── raw/                            ← Source materials (Karpathy wiki)
├── review-system/                  ← Review workflow
└── SOUL.md, AGENTS.md, FRAMEWORK.md, SOURCES.md, etc.
```

### Storage Audit Results
- Total ~/.openclaw/: 1.5GB
- MC Production: 822MB (660MB node_modules)
- MC Dev: 676MB (659MB node_modules)
- assets/: 5.4MB
- data/: 384KB
- System disk: 228GB total, 16GB used (57%)
- WD MyCloud: NOT mounted — backup not running

### GitHub Repos
- `openclaw-missionscontrol` — shared remote for workspace, MC prod, and MC dev
  (needs separation — workspace should have its own repo)

### Issues Found
- WD MyCloud not mounted — backups not running
- All three local repos point to same GitHub remote
- Workspace has 13 uncommitted changes (data files, logs)
