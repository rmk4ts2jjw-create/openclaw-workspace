# 2026-05-14 — Daily Log

## Mission Control Dashboard — Session Summary

### Fixes Applied

1. **Dashboard layout** — Enlarged from `max-w-6xl` to `max-w-[1650px]`
2. **Agent sprite positioning** — Fixed from fixed pixels to percentages for responsive scaling
3. **Agent sprites** — Replaced with individual files from `owl_station_sprites` folder (320×240 each)
4. **Tasks page loader error** — Added missing `useRef` import
5. **Scheduler page** — Created `/api/crons` endpoint, fixed `execSync` PATH issue, now shows all cron jobs with toggle switches
6. **Scheduler filter** — Added All/Active/Disabled/Errors filter buttons on Cron Jobs tab
7. **Scheduler error feed** — New "Error Feed" tab showing failed cron jobs with Investigate/Resolved/Skip/Undo buttons
8. **Error persistence** — Error statuses saved to `cron-errors.json` via `/api/cron-errors` endpoints
9. **Incident action buttons** — Run (→ in_progress/purple), Completed (green), Skip (grey), Undo
10. **Incident action persistence** — Status changes saved to `incidents.json` via `/api/incidents/update`
11. **Duplicate task prevention** — Server-side dedup in `/api/tasks/add`, client-side loading state on Run button
12. **Dispatch labeling** — Tasks from incident actions labeled `[Dispatch]` with violet badge
13. **Activity page** — Added "Now Running" and "Up Next" panels showing in-progress and backlog tasks
14. **Model Usage page** — New `/usage` page with token/cost breakdown from OpenRouter API
15. **Global refresh** — Increased from 15s to 5s

### Known Issues / Pending
- Sprite images: Current ones from `owl_station_sprites` are placeholder quality — Andre finding better ones
- Model Usage page: Needs `OPENROUTOR_API_KEY` env var set for live data
- Cron job `ed4b4223` (Wiki Lint): Has module error `Cannot find module './dist/warning-filter.js'` — OpenClaw bug
- Dashboard visual overhaul still pending — Andre has more details to follow

### Files Modified
- `src/routes/__root.tsx` — Layout width, refresh timer
- `src/routes/calendar.tsx` — Complete rewrite with tabs, filters, error feed
- `src/routes/tasks.tsx` — useRef import, task card styling
- `src/routes/incidents.tsx` — No changes (data fix only)
- `src/routes/activity.tsx` — Added in-progress/up-next panels
- `src/routes/usage.tsx` — New page
- `src/components/Dashboard.tsx` — Sprite positioning fix
- `src/components/IncidentDetailsDrawer.tsx` — Action buttons, undo, in_progress state, persistence
- `src/components/AppSidebar.tsx` — Added Model Usage nav item
- `src/lib/crew.ts` — Updated sprite imports
- `src/lib/server-data.ts` — Added in_progress to action status type
- `src/server.ts` — Added /api/crons, /api/cron-errors, /api/usage, /api/incidents/update endpoints
- `src/routes/api/save-tasks.tsx` — No changes
- `data/incidents.json` — Restored corrupted INC-001
- `data/tasks.json` — Cleaned up duplicate tasks
