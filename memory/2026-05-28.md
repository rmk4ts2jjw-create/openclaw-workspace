# 2026-05-28 — Daily Log

## Morning Session

### Infrastructure
- **Vite dev server down** — port 3000 not responding. LaunchAgent had died (exit code 143/SIGTERM, TimeMachine permission error).
- **Fixed start-mc.sh** — was using `sed -i ''` to modify build-info.ts in-place on every start (corrupting git state). Now uses `VITE_*` env vars instead.
- **OpenClaw updated** — 2026.5.20 → 2026.5.26
- **LaunchAgent reloaded** with ThrottleInterval increased to 10→30s
- **routeTree.gen.ts** added to .gitignore

### Code Review — All Recommendations Implemented
1. ✅ Removed `/home` test route (leftover duplicate)
2. ✅ Cleaned up `sprites-old/` (12 files, 1.4MB)
3. ✅ Renamed `calendar.tsx` → `scheduler.tsx` (matches sidebar label)
4. ✅ Added "Agent Office" nav item to sidebar; `/spacestation` redirects to `/visual`
5. ✅ Made index page dynamic — uses `getAgentStatus()` instead of hardcoded agents
6. ✅ Added atomic write + in-memory lock to save-tasks API
7. ✅ Added atomic write to `saveTasks` server function in server-data.ts
8. ✅ Migrated build-info.ts from hardcoded values to `import.meta.env.VITE_*`
9. ✅ Renamed all API route files from `.tsx` to `.ts`
10. ✅ Added warning header to RoomCanvas.tsx (hand-tuned pixel art, don't auto-refactor)

### Round 2b — Server-Data Module Split
- Split server-data.ts (1037 lines) into 14 domain modules + barrel export
- All TypeScript compiles clean (0 errors)
- All routes verified 200
- Removed dead code: memory-data.ts (had hardcoded sample data, never imported)
- Fixed hardcoded openclawVersion → reads from config dynamically
- Activity feed now includes completed tasks, open incidents, cron errors (not just file mtimes)
- Router invalidation slowed 15s → 60s
- Added aria-labels + aria-current to sidebar navigation

### Round 3 — Aider-assisted Scheduler Split
- Used Aider (v0.82.3, model: owl-alpha) for the first time
- Split Scheduler page: 559 → 319 lines
- Created useCronData.ts hook (fetching, polling, toggle, error management)
- Created CronJobCard.tsx component
- Aider worked well for this kind of structured refactor

### Remaining Nice-to-Have (skipped)
- Dark/light theme toggle — needs design work
- Memory page content backfill — daily logs are auto-generated health checks, would need richer source material

### Remaining Cosmetic Issues (not critical)
- API route files show "does not export a Route" warnings in Vite dev mode (TanStack Start v8 cosmetic warnings — they work fine, just noisy)

### Git
- Committed and pushed all changes (commit: 21ea161)
