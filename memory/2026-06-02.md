# 2026-06-02

## Dashboard Fixes (Andre request)
- Removed obsolete Sprite Generator page (/sprites route + component + sidebar entry)
- Fixed Vite cache corruption causing 504 "Outdated Optimize Dep" errors
- Committed and pushed to main

## StationRoom.tsx Fix (Andre escalation ~08:32)
- Fixed `ReferenceError: Cannot access 'accentHex' before initialization` — dead line calling undefined `oklchToHex()`
- Build passes, all routes 200

## Incident → Operations Workflow Integration (~08:45–09:30)
- Extended types: IncidentAction (7 states, taskId, assignee), Incident (AWAITING_REVIEW, linkedTasks), Task (incidentSource)
- Created `src/lib/incident-actions.ts`: response action generation engine
- Rebuilt `IncidentDetailsDrawer`: full action lifecycle, Operational Work panel, submit-for-review workflow
- Added `incidents-status.ts` and `incidents-resolve.ts` API endpoints
- Build passes, committed + pushed (`1a83382`)

## IncidentDetailsDrawer Hooks Fix (~10:15)
- Fixed "Rendered more hooks than during the previous render" crash
- Root cause: second `useEffect` was below the `if (!incident) return null;` guard
- Moved all hooks above the early return, with internal guards inside callbacks
- Committed + pushed (`beabd8a`)

## Stalled Task Investigation & Fix (~11:20)
- **Stalled task**: `task-mp7d3s9w-1lmf` — in_progress 12+ hours, archivist never actually started work
- **Why stall detection failed**:
  1. `work-dispatcher.sh` only checked `IN_PROGRESS > 0` and exited — no staleness logic
  2. Heartbeat relied on `startedAt` (empty) instead of `lastActivity``/history` timestamps
  3. `ts: "just now"` is not a parseable ISO timestamp
  4. `stalledAt``/stallNotified` fields existed in type but nothing ever wrote them
- **Fixes applied**:
  - Reset task to backlog with history entry
  - Rewrote `work-dispatcher.sh` with proper stall detection (>2h = stale, auto-reset)
  - Dispatcher now tries `lastActivity` → `ts` → `startedAt` → `history[].ts` for staleness
  - Updated HEARTBEAT.md with explicit stall detection rules
  - Dispatcher now sets `lastActivity` on dispatch

## tasks.json Destruction & Restoration (~12:00)
- Discovered tasks.json was overwritten — 31 done tasks wiped, only 3 remained
- Root cause: MC frontend save-tasks did a full-array overwrite with its in-memory state
- Restored full 34-task state from git history (`git show HEAD~1:data/tasks.json`)
- Opencode task (`task-mpwfiydb-6khn`) was also missing — re-added manually
- **Fix**: Rewrote both `src/routes/api/save-tasks.ts` and `src/server.ts` to use merge-based saves:
  - Read current disk state, merge client changes, preserve disk-only tasks
  - Client wins for tasks it knows about; disk-only tasks preserved as-is
  - Atomic write (write to .tmp, then rename)

## Error Feed Flip-Flop Fix (~12:30–13:30)
- Error Feed task (`task-mp7cbyxz-0ztq`) keeps alternating between backlog and in_progress
- Dispatcher picks it up → heartbeat resets to backlog → dispatcher picks it up again
- Added `stalledAt` field — dispatcher now skips tasks with `stalledAt` set
- Dispatcher also checks dispatch count (3+ auto-dispatches with no progress = skip)
- **Race condition discovered**: heartbeat and dispatcher can run in same minute, heartbeat writes `stalledAt` after dispatcher reads file
- **Protected-fields merge**: Added to both save-tasks endpoints — `stalledAt` and `stallNotified` fields on disk are not overwritten by client state

## OpenCode Investigation (~13:15–15:30) — COMPLETE
- OpenCode v1.15.13 installed at `~/.opencode/bin/opencode`
- Picks up `OPENROUTER_API_KEY` automatically — no login needed
- **Config solved**: `~/.config/opencode/opencode.json` validates successfully:
  - Default model: `openrouter/qwen/qwen3-coder:free`
  - Title/small agent: `openrouter/moonshotai/kimi-k2.6:free`
  - Eliminates 402 error from claude-haiku-4.5 title generation
- **Schema**: `model` is a string, `provider.openrouter.models` is `{key: {id, name}}`, agents: `{plan, build, small, title}`
- **Model test results**:
  - `openrouter/free`: ✅ Works for simple prompts (auto-router, best rate limit handling)
  - `qwen/qwen3-coder:free`: ⚠️ Rate-limited, sessions SIGTERM after >2min
  - `moonshotai/kimi-k2.6:free`: ⚠️ Rate-limited, sessions hang
  - `google/gemma-4-26b-a4b-it:free`: ❌ Rate-limited (hangs)
  - `meta-llama/llama-3.2-3b-instruct:free`: ❌ No tool use support
  - `meta-llama/llama-3.3-70b-instruct:free`: ⚠️ Rate-limited
  - `liquid/lfm-2.5-1.2b-instruct:free`: ❌ No tool use support
- **File editing**: openrouter/free found files but too slow (SIGTERM). qwen3-coder timed out.
- **Verdict**: Config correct. Free tier rate limits make sustained coding unreliable. Simple Q&A works.
- **Recommendation**: Add $5-10 OpenRouter credits for reliable coding, or use openrouter/free for simple tasks
- **owl-alpha**: Not in OpenCode's model catalog (OpenRouter routing model, not standard provider)

## Task Status (end of day)
- Error Feed (mp7cbyxz-0ztq): in_progress (flip-flopped >=15 times at 14:29). Has `stalledAt`. Awaiting human triage.
- Completion Summary (mp7d3s9w-1lmf): Backlog. Has `stalledAt`.
- Opencode Coding Buddy (mpwfiydb-6khn): Backlog. Investigation complete; tool works but free models rate-limited.
- All other tasks: Done.

## OPS-002: Incident → Task Linking Implementation (~17:00)
- **Architecture decision**: `Task.linkedIncidentId` is source of truth. `Incident.linkedTaskIds` is derived at render time.
- **Triage workflow**: Auto-created incident tasks start in `triage` status (not backlog). New Triage Kanban column.
- **Files changed**: types.ts, tasks.tsx, incidents-create.ts, incidents-timeline.ts (new), auto-detect-incidents.sh
- **Key features**:
  - Bidirectional linking: task→incident via linkedIncidentId, incident→tasks via runtime derivation
  - Task detail panel shows linked incident (title, severity, status, owner)
  - Incident detail drawer shows linked tasks with status badges
  - Task status changes auto-append to incident timeline via /api/incidents-timeline
  - Auto-detect creates linked tasks + severity-based response actions
  - Incident resolution remains separate from task completion
- **Build**: passes, all routes 200
- **Commits**: `1dc5d25` (submodule), `a6b28b7` (auto-detect)

## Heartbeat Notes for 2026-06-02
- **Stall Detection**: Error Feed task (mp7cbyxz-0ztq) is 1h+ stale (lastActivity=13:29). Not reset yet (30–2hr window). Protected `stalledAt` prevents dispatcher from dispatching until cleared.
- **Platform Stability**: All routes return 200; Spacestation loads correctly (redirect expected). Dev server running on port 3000.
- **OpenClaw Behavior**: Merge-based save logic functional. Protected `stalledAt` prevents task resets outside cron/agent flow
- **OpenCode Limitations**: Free models (kimi-k2.6:free) remain viable despite rate limits but require title model config fix
## Strategic Backlog Planning (~15:40)
Created 37 new backlog tasks decomposing the Tasks/Incidents/Scheduler area into a 6-phase operational command system:

- **Phase 1 — Ops Foundation (5):** Auto-incident creation, incident→task linking, response action lifecycle, operational work panel, awaiting review flow
- **Phase 2 — Runbooks (5):** Runbook data model, runbook UI, incident type mapping, suggested action generation, task creation from runbooks
- **Phase 3 — Asset Registry (5):** Asset schema, asset view, asset health, incident-asset linking, dependency mapping
- **Phase 4 — Timeline (5):** Unified event model, incident/scheduler/task events in timeline, commander timeline view
- **Phase 5 — Autonomous Ops (5):** Automation health, retry policies, circuit breakers, provider cooldowns, auto remediation
- **Phase 6 — Knowledge System (5):** Lessons learned, knowledge capture, runbook evolution, framework sync, searchable knowledge base
- **Enhancements (7):** Category badges, priority/severity system (P1-P4), temporal intelligence, agent accountability, incident atmosphere, queue/flow visualization, operational readiness dashboard

Priority distribution: P1 × 5, P2 × 17, P3 × 15, plus 1 existing P3 (Completion Summary).
Total backlog: 38 tasks targeting ~6-12 months of Mission Control evolution.

## Kanban Layout Fix (~18:10)
Andre reported that Awaiting Review and Done columns were hidden behind horizontal scroll.

**Audit findings:**
- `awaiting_review`: 0 tasks — completely unused, dead column
- Other statuses: triage(1), backlog(37), in_progress(1), done(34)
- Root cause: `md:grid-cols-3` with 5 columns = last 2 columns hidden

**Fix applied:**
1. Removed `awaiting_review` from COLS array (status kept in type for backward compat)
2. Changed grid from `md:grid-cols-3` to `md:grid-cols-4`
3. Added `reviewed?: boolean` field to Task type
4. Added green REVIEWED badge on Done tasks (shown inline next to "Done by")
5. Added Review/Reviewed toggle button in TaskDetailPanel action area
6. All 4 columns now visible: Triage → Backlog → In Progress → Done

**Files changed:** `src/routes/tasks.tsx`, `src/lib/server-data/types.ts`
**Build:** ✓ All routes 200
**Commit:** `7c38fc7`

## Incident → Task Linking (OPS-002, ~17:00-17:50)
Full bidirectional linking system implemented:

- **Source of truth:** `Task.linkedIncidentId → Incident.id` (not `Incident.linkedTaskIds`)
- **Auto-detect creates linked tasks:** `auto-detect-incidents.sh` now creates a task in `triage` status per incident
- **UI incident creation:** `/api/incidents/create` also creates linked task via `incidents-create.ts`
- **Response actions:** Auto-generated via `generateResponseActions()` for UI-created incidents
- **Timeline push:** Task status changes auto-append to linked incident timeline via `/api/incidents/timeline`
- **Derived linked tasks:** `IncidentDetailsDrawer` derives linked tasks from `tasks.json` at render time
- **Task detail panel:** Shows linkedIncident badge with severity, status, owner
- **Triage column:** Added as first Kanban column for incident-response workflow
- **Separate resolution:** Incident resolution stays independent from task completion

**Architecture:** Error → Incident → Response Task → Investigation → Review → Resolution

## Progress Bar Fix + In-Progress Audit (~19:00)

**Audit of 4 in_progress tasks — ALL ABANDONED:**
- `task-inc-040-f114` (Investigate: INC-040, lifesupport, 37min) — no agent working
- `task-inc-041-ae7e` (Investigate: INC-041, lifesupport, 37min) — no agent working
- `task-mpwz0zii-2ort` (Move Incident ShortCut, monkey, 30min) — no agent working
- `task-inc-042-3152` (Investigate: INC-042, monkey, 18min) — no agent working

**Root cause:** Dispatcher moves tasks to in_progress with "Agent starting…" but no agent process picks them up. Only active session was this one (OWL). Monkey/lifesupport agents are sub-agents spawned per-request, not persistent workers.

**All 4 reset to backlog** with history noting the dispatcher-agent handoff gap.

**Progress bar fixes:**
- Removed fake "Agent starting…" pulse animation
- Shows "Waiting for agent..." (grey) when no agent has touched the task
- Shows amber stall warning with duration when >30min no activity
- Only shows real progress % when agent explicitly sets it
- Removed calculateProgress() fake value derivation (was returning 50/60/75 based on recency)
- Added `dispatcherDefaultStep` detection to distinguish real agent work from dispatcher default

**Commits:** `41e819e` (reset tasks), `0c828d5` (progress bar fix)
## Daily Station Check — 2026-06-02 23:00
- System: 1.90 load, 53% disk, uptime: up 13 days, 10:53, 2 users, load averages: 1.90 1.98 1.72
- Git: no changes
- Tasks: 0 active, 0 awaiting review, 36 archived
- Backup: mounted: /Volumes/OpenClaw-WD | no backup files found ⚠️
- Status: ✅ ALL SYSTEMS NOMINAL
